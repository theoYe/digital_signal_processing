close;
clear all;

A = 444.128;
alpha = 50*sqrt(2)*pi;
Omega_0 = 50*sqrt(2)*pi;


% plot x(t)
tp = 0.01 % 间隔
tmax = 2  % 最大绘制点2s
tm =  [0:tp:tmax ]      % 绘制范围, 从0开始, sheng'lu

x_at = A.*exp(-alpha.*tm).*sin(Omega_0.*t)  % 

